Run `node scripts/verify-slugs.mjs` with the Bash tool. Report which companies verified (with ATS and live posting count) and which did not. Remind Chase that unverified companies stay in the chat deep sweep and can never produce puller rows. If arguments were given (company names), first add them to config/companies.json with sensible candidate slugs across all four ATS types, then verify. $ARGUMENTS
